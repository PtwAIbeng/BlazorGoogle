@inject BlazorAppAuth.Utils.AuthenticatedUserInfo UserInfo

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Bassics.Web</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        @* 
        @if (UserInfo.GetAccount() != null && UserInfo.GetAccount().HasPermission(PermissionType.SystemAdmin))
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="accounts">
                    <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Manage Accounts
                </NavLink>
            </div>
        } *@
    </nav>
</div>

